// Placeholder for OpenTelemetry Node SDK setup — wired up once the backend's
// tracing pipeline (plan §3 observability) is in place and there's a collector
// endpoint to point at. Kept as an explicit no-op rather than skipped entirely
// so the instrumentation.ts import path is exercised from Phase 0 onward.
export {};
